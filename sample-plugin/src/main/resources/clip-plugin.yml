name: '${pluginName}'
version: '${version}'
main: '${group}.${packageName}.plugin.${pluginName}Plugin'
api-version: '${paperVersion}'
libraries:
  ${pluginLibraries}
  - '${group}:${packageName}-core:${version}'